# --- Database Credentials ---
DB_USER=pgadmin
DB_PASS=change_me
DB_NAME=weather_db

# --- Network Configuration (Macvlan) ---
# Assign static IPs that match your specific subnet configuration
DB_IP=192.168.1.140
ETL_IP=192.168.1.141
# The host IP for the python script to connect to (usually same as DB_IP)
DB_HOST=192.168.1.140

# --- Geographic Target ---
# Decimal coordinates for the weather forecast
# Example: Thessaloniki, Greece
LATITUDE=40.64
LONGITUDE=22.94

# --- System & Timezone ---
TZ=Europe/Athens

# --- Docker Paths ---
# Adjust these paths based on your host system (TrueNAS vs Standard Linux)
# For local testing, you can use relative paths like ./postgres_data
VOLUME_PATH=/mnt/apps_pool/weather_pipeline/postgres_data
BUILD_PATH=/mnt/apps_pool/weather_pipeline/app